const myName = "Lucas";
const birthCity = "Cambará";
birthCity = "Andirá"; //O valor da variável não é alterado, pois o const não permite alteração.
let birthYear = 2001;  
birthYear = 2030; //O valor da variável é alterado, pois o let permite alteração.

console.log(myName);
console.log(birthCity);
console.log(birthYear);